name: validate
on:
  pull_request:
    branches:
      - $default-branch
  push:
    branches:
      - $default-branch
jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make compile
